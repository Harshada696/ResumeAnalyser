services:
  - type: web
    name: ResumeAnalyserAi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    rootDir: backend
    autoDeploy: true
